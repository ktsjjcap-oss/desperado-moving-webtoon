# RELEASE BLOCKER — TRUE ACTING ASSETS REQUIRED

The current deployed pilot remains rejected as a Commercial Moving-Webtoon Master.

Reason: existing source art is primarily flattened panel imagery. Camera transforms, parallax and overlays cannot satisfy the v2 definition by themselves. The next build must use scene-specific acting states or genuinely separated motion layers for character/object changes.

Do not declare PASS from Playwright transform checks alone. Production promotion is blocked until the Director evidence visibly demonstrates: corridor approach/stop/reaction/arm-stop; footstep shadow/reaction; door reach/handle/resistance/impact; and staged Silver Lily reveal.

This blocker intentionally prevents another false completion report.